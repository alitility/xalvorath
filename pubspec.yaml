name: xalvorath_pc
description: Xalvorath PC modular music app
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  file_picker: ^10.3.3
  flutter:
    sdk: flutter
  just_audio: ^0.10.5
  just_audio_windows: ^0.2.2
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

